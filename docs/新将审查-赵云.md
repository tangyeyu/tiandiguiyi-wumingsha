# 新将审查：赵云（蜀·4血）

> 状态：**口径已完备（v2）** —— 可进入实现。
> **强度取向（用户明确）：娱乐向超模** ⇒ 本文只记录"语义歧义/规则冲突/可实现性"，
> 不再就"偏强"提削建议（除非出现自相矛盾或无法实现）。

---

## 一、技能文本（按全部已裁定口径定稿）

### 武翊（卡面一个技能名；实现上允许内部分装多个触发挂载点）

1. 你可以将一张【杀】当【闪】、【闪】当【杀】使用或打出。
2. 回合开始时，你从下列四项中选择一项，**先从牌堆中查找**该装备，牌堆中没有则从弃牌堆中查找，
   获得之；**两处都没有则该次不获得**：
   ①【进攻马】（即 -1马：你到他人的距离 -1）
   ②【防御马】（即 +1马：他人到你的距离 +1）
   ③【武器】
   ④【防具】
   > 判定口径：**所有 -1马都是进攻马；所有 +1马都是防御马**（按装备子类型判定，不需牌名清单）。
3. 你的手牌上限 +X（X = 你装备区内**已装备的栏位数**）。
   装备区为**标准四栏位**（武器 / 防具 / +1马 / -1马）。**不设宝具栏**。
4. 你造成伤害时（**不限伤害来源**），你可以弃置一张**武器牌**（装备区内或手牌中，两处均可），
   令此伤害 +1。**同一回合内不能多次发动**；同一次伤害**不能叠加**（仅指本项内部）。
5. 你受到伤害时，你可以弃置一张**防具牌**（装备区内或手牌中，两处均可），**完全抵消此伤害**
   （此伤害不发生 ⇒ 不算受到伤害，可躲过"受到伤害后"类触发）。
   **两处都没有相应牌时，不能发动此项。**
6. 当你装备区内有**进攻马**时，**每局第一次**你受到伤害，**免疫之**（此伤害不发生）；
   然后**你可以选择**弃置装备区内的进攻马。
   **当你装备区内的进攻马离开装备区时，你摸两张牌**（含被他人弃置/夺取、含装备区内的替换）；
   **若你保留该马，则不摸牌**。
   **同一栏位重新装备（含换另一张马）即刷新"每局第一次"**；**被他人夺走时同样刷新**。
7. 当你装备区内有**防御马**时，**每局第一次**你造成的伤害 +1，然后**你可以选择**弃置装备区内的防御马。
   **当你装备区内的防御马离开装备区时，你摸两张牌**（同上口径）。
   **同一栏位重新装备即刷新"每局第一次"**；**被他人夺走时同样刷新**。
8. 你使用或打出过的**基本牌牌名数**（**不同牌名**；**【火杀】【雷杀】不计为独立牌名**）
   **本局累计**达到：
   - 1：你使用基本牌无次数限制；
   - 2：你使用基本牌无距离限制；
   - 3：当你获得/失去装备时，你摸一张牌。
   达到 3 时 ①②③ **全部生效**（此后持续生效）。
   「获得/失去装备」**包含「移动」（装备区内部的替换与移出）**；**替换装备会同时触发两次**
   （旧牌失去 + 新牌获得）。**不包含「手牌 → 装备区」的放入**（该情形只算"获得"以外的操作，见下"裁定"）。

### 摧锋

1. **每轮限一次**。每回合结束时，你可以摸一张牌并执行一个额外的回合
   （额外回合**包含判定阶段与摸牌阶段**）。
   **额外回合结束时，不能再发动本项。**
   > 额外回合内**可以再次触发 `武翊2`**（回合开始拿装备）—— 用户明确。
2. 每回合结束时，你可以从装备栏或手牌中弃置一张装备牌（**弃置是代价**），
   然后你可以**视为对当前回合角色使用一张【决斗】**，此【决斗】造成的伤害 +X
   （X = 本回合所有角色使用或打出【杀】的次数；**同一张【杀】指定多个目标按 1 次计**；
   **X 至多为 3**）。
   该【决斗】**不能以你自己为目标**；**至少造成 1 点伤害**
   （**若被【无懈可击】取消，则保底不成立**）。
   **若你没有装备牌可弃，则不能发动本项。**
3. 上述两项**在同一回合结束时都可以发动**。

---

## 二、裁定记录（全部条目，作为实现依据）

### 武翊

| 条目 | 裁定 |
|---|---|
| 2 获取顺序 | 先牌堆 → 后弃牌堆 → 都没有则不获得 |
| 2 选择方式 | 从四项中**选一项**（用户口径） |
| 3 栏位 | **标准四栏位**；**彻底删除宝具** |
| 进攻马/防御马 | **所有 -1马 = 进攻马；所有 +1马 = 防御马** |
| 4 伤害来源 | 包含非牌来源 |
| 4 弃牌位置 | 装备栏 / 手牌，**两处都可以** |
| 4 叠加 | 同一次伤害不能叠加；**同一回合内不能多次发动** |
| 4/5 无牌可弃 | 不能发动 |
| 5 抵消程度 | **完全抵消**（不算受到伤害） |
| 5 与 6 的关系 | **同一机制，不能叠加**（二者都只是"令此伤害不发生"） |
| 6/7 "第一次" | **每局第一次**；**同栏位重新装备即刷新**；**被夺走时同样刷新** |
| 6/7 离开装备区 | 包含被他人弃置/夺取；包含装备区内替换 |
| 6/7 保留马时 | 不触发摸两张 |
| 8 计数 | 按**基本牌牌名数**；**本局累计**；达到 3 时 ①②③ 全部生效 |
| 8 牌名 | **【火杀】【雷杀】不算独立牌名** |
| 8③ 移动 | **包含「移动」**（装备区内部替换/移出）；**不包含「手牌 → 装备区」** |
| 8③ 与 6/7 叠加 | **能重复触发**（马离开装备区：6/7 摸 2 张 **+** 8③ 摸 1 张 = **摸 3 张**） |
| 4 与 7 叠加 | **同一次伤害可以叠成 +2** |
| 分装 | 卡面一个技能名；实现上**允许内部分装** |

### 摧锋

| 条目 | 裁定 |
|---|---|
| 1 额外回合 | 含判定/摸牌阶段；**结束时不能再发动 1** |
| 1 次数 | **每轮限一次**（原两次，平衡下调） |
| 1 与 武翊2 | 额外回合内**可以再触发 武翊2** |
| 2 X 计数 | 同一张【杀】多目标按 **1 次**计 |
| 2 X 上限 | **至多为 3**（平衡新增） |
| 2 目标 | 不能以自己为目标 |
| 2 保底 | 至少造成 1 点伤害；被【无懈可击】取消则**保底不成立** |
| 2 弃置 | **代价**（取消不返还） |
| 2 无装备可弃 | 不能发动 |
| 两分支 | 同一回合结束时**都可以发动** |

---

## 三、强度与设计评估（按"娱乐向超模"定位）

### 设计思路（优点）

1. **"装备库存管理"内核** —— 装备同时是**手牌上限、进攻资源、防御资源**，
   `武翊2` 每回合补充 ⇒ 独特的资源循环手感。
2. **`武翊` 与 `摧锋` 的张力** —— 装备既增值（3/4/5/6/7）又是消耗品（4/5/摧锋2）⇒ 取舍感强。
3. **`武翊8` 的"牌名成就"** —— 火杀/雷杀不算独立牌名之后，它奖励"用多样基本牌"，
   是**克制型**设计，不会立刻全能。

### 强度定位（明确为娱乐向超模，此处只做说明，不主张削弱）

按全部裁定，本武将的**上限**大致是：

| 维度 | 表现 |
|---|---|
| **防守** | `武翊5` 可**反复完全抵消**（每回合弃防具即可）+ `武翊6` 每局一次免疫 ⇒ 对手输出几乎无效 |
| **进攻** | 单次伤害最高 **+4**：`武翊4` +1、`武翊7` +1、`摧锋2` X（至多3，但那是决斗）／另计 `武翊8①②` 基本牌无限次无距离 |
| **抽牌** | 马离开装备区一次 **摸 3 张**（6/7 的 2 张 + 8③ 的 1 张），而 `武翊6/7` 可**刷新** ⇒ 可反复取用 |
| **回合数** | 每轮最多 2 个回合（正常 1 + 摧锋1 额外 1），额外回合**还能再触发 `武翊2`** |
| **手牌上限** | +4（四栏位全满时） |

⇒ **远超标准 4 血武将**，接近"神将/娱乐向"强度。这是**已确认的设计取向**。

**若要日后收一收，成本最低的两个旋钮**（仅备注，非本次要求）：
- `武翊5` 加"每回合限一次"
- `武翊6/7` 去掉"可刷新"

---

## 四、仍需你补充（只剩实现细节，不影响语义）

| # | 问题 | 说明 |
|---|---|---|
| 1 | `武翊2` 的"选择一项"是**玩家主动选**（弹出四选一）还是**随机**？ | 按"选择"字面与通常做法应是**主动选**，请确认 |
| 2 | `摧锋1` 的**额外回合结束时**，**其他"回合结束"类技能**（如 `摧锋2`、其他武将技能）是否照常触发？ | 你只定了"不能再发动摧锋1" |
| 3 | `摧锋` 两分支的**发动顺序** | 若先发动 2（弃装备）再发动 1（额外回合），`武翊8③` 的摸牌计数会受影响；建议定为"**由玩家自选顺序**" |
| 4 | `武翊5` 的"完全抵消"能否**抵消一次伤害中的 1 点**（多段伤害）？ | 建议写明"**仅能抵消整次伤害**" |
| 5 | `武翊8③` 的摸牌在**同一回合内替换多次装备**时是否每次都触发？ | 按裁定应是每次都触发（无次数限制），请确认 |
| 6 | 是否**现在就实现**？ | 若实现，我按本项目规范（步骤号连续 / filter 只判 event / 检测集中追踪器 / mark:true）落地并跑七件套自检 |

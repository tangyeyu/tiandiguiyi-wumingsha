# 三将开发笔记 · 兵·诸葛亮 / 名·周瑜 / 名·裴秀（2026-09-13）

> 本文收录第三批三将的**引擎事实核实清单（当次对着本机琉璃版 5.5 核实）**、
> **范式与陷阱**、**事故复盘**。定稿文本见 [`docs/新将文本定稿-20260913.md`](新将文本定稿-20260913.md)。
> 行号随版本会漂移，机制契约以结论为准。

---

## 1. 文本校对流程（先于实现，强烈建议保持）

手写卡面 → **逐字转录** → **两轮校对**（第一轮：转录+歧义/缺失/矛盾清单 → 用户逐条裁定；
第二轮：修订稿复核残余问题）→ **定稿文档**（含裁定记录表）→ 才开始实现。

本轮实践证明价值：三将共发现 **8 个硬伤**（X 未定义、英姿条件互斥、反间
"失去全部"与"大于两张"矛盾、技出不可实现、爵制编号错乱、携图缺字、
完图语句破碎、兵权时间线），全部在写码前关闭。实现期返工为零。

---

## 2. 引擎事实核实清单（本批新增，全部当次核实）

| # | 结论 | 证据位置 | 用在 |
|---|---|---|---|
| 1 | enable 三形态匹配规则：函数→`info.enable(event)`；**数组→`contains(event.name)`**；字符串 `'phaseUse'`→特判 `event.type=='phase'`；其余字符串→`==event.name`。出牌阶段窗口名是 `'chooseToUse'`——**数组里写 'phaseUse' 永远不命中按钮** | game.js:42917/42925/42926 | 启备事故 |
| 2 | 无懈可击的响应窗口 = `chooseToUse` + `type:'wuxie'`，**不是** chooseToRespond | game.js:16261/33424 | 启备 |
| 3 | 无懈是 **useCard**（"使用"）而非 respond ⇒ `onrespond` 不触发；扣费/记账挂 `onuse`（chooseToUse 结果回填时调用，`result.card` 为产出牌） | game.js:25524 / 20010-20012 | 启备 |
| 4 | `dialog.add` 的 else 分支把数组当 **[list, type] 对**拆开——裸字符串数组会让牌名被当成按钮 type，`ui.create.button` 的 switch 无匹配 ⇒ node 未创建 ⇒ `addEventListener` 崩溃 | game.js:32625 / 55085-55305 | 破竹/武库事故 |
| 5 | 牌名选单标准形态：`chooseButton([提示, [list,'vcard']])`；vcard 按钮 link 是 `[type,'',name]` 三元组，牌名取 `[2]` | clan.js:235 / refresh.js:4133 / game.js:55129 | 破竹/武库 |
| 6 | chooseButton 的 **sha 分支必须先推普通杀**再推属性变体——漏推则南蛮/决斗等索要普通杀的窗口无普通杀可选 | shiji.js spmiewu 当次核对 | 启备 |
| 7 | **「当任意牌名」类技能的官方范式 = spmiewu**：`enable:['chooseToUse','chooseToRespond']` + `chooseButton`（dialog 内 `event.filterCard` 逐名校验只列合法牌名）+ `backup.precontent` 扣费 + `hiddenCard`/`skillTagFilter` | shiji.js:5565+（spmiewu） | 启备重写 |
| 8 | chooseControl：控件列表**没有 `'cancel2'` 就没有取消按钮**；点取消时 `result.control=='cancel2'`。读 `result.index` 并在非 number 时回退 0 的写法，会把"取消"执行成"选第一项" | game.js:18084-18280 / clan.js:2018-2033 | 陆抗三处事故 |
| 9 | 「发动技能」探针 = **logSkill 时机**：每次产生战报的技能发动都派发事件（`event.skill`=技能 id），含锁定技自动触发；`popup:false` 的静默内部技能**不派发** ⇒ 天然防自触发 | game.js:27175-27193 / clan.js:288 | 善谋/技出/空城阴 |
| 10 | 一个技能可挂**多时机 trigger**，content 内按 `event.triggername` 分支（官方惯例）——技能栏每个卡面技能只需一条目 | game.js 触发链 / diy.js:5818 | 三将合并式重构 |
| 11 | useSkill 的 content 在技能有 viewAs 时**照常执行**（viewAs 只影响弃牌展示）——content 型与 viewAs 型可在 enable 上共存 | game.js:25642-25655 | 技出合并 |
| 12 | `onrespond` 只在 respond 事件调用；**useCard 路径（无懈）要挂 `onuse`** | game.js:20010-20012 / 25524 | 启备 |
| 13 | viewAs 函数在玩家点"确定"的 UI 回调里同步求值（game.js:58653 ok → 58671 调用）——**里面不能开对话框**；要选牌名用 chooseButton 范式 | shiji.js spmiewu | 启备 |
| 14 | `zhuSkill` 只在玩家 **init** 时过滤（非主公直接不加技能）；外部若绕过 init 塞入则不受防——filter 里补 `isZhu2()` 兜底 | game.js:22930-22933 / 23087-23089 | 兴汉事故 |
| 15 | `addTempSkill` 缺省过期 `['phaseAfter','phaseBefore']`=回合结束；第 4 参 addToSkills=true 时**不入 player.skills**（tempSkills 移除循环靠 `notemp.contains` 跳过 skills 内的技能，temp 挂载不入列才会正常过期） | game.js:28663 / 32427 / 28169 | 章武/彰才 |
| 16 | **mod 型子技能绝不能列进武将数组**（数组=开局常驻）；trigger 型必须列（filterTrigger 只查数组）。mod 动态挂载即可生效：getSkills() **包含 tempSkills** | game.js getSkills/checkMod 当次核实 | 无限出杀事故 |
| 17 | 限定技自管口径：`limited:true` + `init` storage=false + filter 检查 + content 置 true（引擎不再代管 limited 的触发门槛） | 引擎惯例 + 本包核实 | 九伐 |
| 18 | "每回合限 X 次"两种实现：enable 技能挂 `usable:静态数字`（引擎只认静态数）；触发/转换类用 storage 计数 + `tdgx_turn_reset` 每回合清零 | game.js:15528 / 四将笔记 #6 | 各技能 |
| 19 | 入队事件时序：content 内调 `gain()/discard()/useCard()` 是**入队事件，步骤边界才落地**——同一步内 countCards/getHistory 读到旧值。状态判定放下一步（反间终止判定事故）；"额外结算"嵌套立即执行时 getHistory 查不到 ⇒ 计数用手动 storage（情势无限连锁事故） | 本包两事故复盘 | 反间/情势 |
| 20 | 双监听同翻面：两个技能监听同一时机都翻转同一状态 ⇒ 状态原地踏步 + 另一面强制对话框每次弹出。"额外触发"类只结算效果**不翻转** | 技出·承事故复盘 | 技出 |
| 21 | logSkill 会为**每次技能发动**入 `getHistory('useSkill')`——跨回合"是否发动过某技能"可用它，不用自建旗标 | game.js:27193 | （备用） |
| 22 | `game.createEvent` **自动把新事件压入当前事件的 next 队列**（game.js:40348）——content 内创建事件后想改排队位置，必须先 `event.next.remove(next)` 再推目标队列，否则**同一事件执行两次**（情势"额外结算"×2 事故；官方重排队先例 game.js:27189-27191） | game.js:40330-40348 | 情势 |
| 23 | 琉璃版的事件**不写 `.parent`**（全文件零赋值）——`getParent()` 返回**空对象**，`getParent().player` 为 undefined。AI 询问的 ai 回调里用它会让回调抛异常、询问永不解决（兵权送兵卡死事故）。要在回调里拿上下文，用 `.set('key', 值)` 挂到事件上再读 `_status.event.key` | game.js getParent 实现（当次核实） | 兵权 |
| 24 | **非 forced 的 trigger 每次时机命中都会弹「是否发动」询问**——高频时机（logSkill/damageBegin2/gainAfter 等）的分支必须 `forced:true`+`popup:false`+`direct:true` 静默自动，否则玩家被询问框淹没（技出·承「一直循环选技能」事故）。低频主动类（限定技询问）保留非 forced | 引擎触发询问机制（当次核实） | 技出 |

---

## 3. 范式与陷阱（速查）

### 3.1 「当任意牌名使用或打出」⇒ 照抄 spmiewu（shiji.js）
```
enable: ['chooseToUse','chooseToRespond']   // 数组必须含 'chooseToUse'（见事实1/2）
filter: 备>0 && 未用过 && lib.inpile 里有名字能过 event.filterCard
chooseButton: {
    dialog: 遍历 lib.inpile，event.filterCard({name:i},...) 逐名校验 ⇒ [list,'vcard']
    filter/check/backup(links)⇒{ viewAs:{name:links[0][2]}, precontent:扣费+temp标记 }
    prompt(links)
}
hiddenCard / ai.skillTagFilter：响应可用性判定用
```
坑：①sha 分支先推普通杀再推属性变体；②裸字符串数组会崩（事实4）；③扣费放
precontent，别放 content（viewAs 技能不走 content）。

### 3.2 「当你发动技能后」⇒ trigger `logSkill`
```
trigger: { player: 'logSkill' }   // event.skill = 技能 id
```
含锁定技自动触发；不含 popup:false 的静默内部技能（正好防自触发）。
响应器自身务必 popup:false + direct:true。

### 3.3 多时机合并式（技能栏瘦身）
```
trigger: { global:'roundStart', player:['phaseEnd','phaseUseBegin'] }
content: 按 event.triggername 分支
```
filter 也要按 triggername 分支（每时机各自的门槛）。enable+trigger 共存时，
phaseUse 按钮路径的 content 内 `event.triggername` 为 undefined。

### 3.4 「本回合限一次」⇒ precontent/content 置 temp 标记
`addTempSkill('xx_used')`（缺省回合结束过期），filter 查 `hasSkill('xx_used')`。
不要用 storage 手工记账（要自己找复位点）；mod 型常驻门槛用 storage+门控。

### 3.5 转换技简实现
不需要引擎特殊字段：storage 翻面 + 效果按面分支，初始阳面。
转换时机写清楚（"当你手牌数变为0或从0改变时"这类）。

### 3.6 取消语义
chooseControl 要可取消 ⇒ 控件里加 `'cancel2'` 并判 `result.control=='cancel2'`。
**禁止** `result.index` 回退 0 的写法。

### 3.7 多时机合并的边界（九伐事故）
带 `skillAnimation`（全屏扫屏动画）或非 forced（要弹"是否发动"）的技能，
**不能**并入静默追踪分支——合并后每次有人用/弃【杀】都会空放一次动画且无选项
（用户实测「一进游戏就技能进度条但没选项」）。静默追踪器保持独立子技能
（forced+direct+popup:false）。合并前先问：这个 trigger 的每个分支要不要
动画/询问/门槛一致？不一致就不合并。

---

## 4. 事故复盘（本批 11 起，全部闭环）

| 事故 | 根因 | 修复 | 教训 |
|---|---|---|---|
| 刘备无限出杀 | mod 子技能被列进武将数组=开局常驻 | 移出数组，动态挂载 | 事实16 |
| 兴汉非主公生效 | zhuSkill 仅 init 过滤，获得路径未定位 | filter 补 isZhu2 兜底 | 事实14 |
| 破竹/武库崩溃 | dialog.add 把裸字符串数组当 [list,type] | [list,'vcard'] 对 | 事实4/5 |
| 南蛮缺普通杀 | chooseButton sha 分支漏推普通杀 | 补 push | 事实6 |
| 启备出牌阶段不可用 | enable 数组无 'chooseToUse' | 照 spmiewu 重写 | 事实1/7 |
| 启备无懈不可用 | 无懈窗口是 chooseToUse；无懈是 useCard | enable+onuse | 事实2/3/12 |
| 陆抗卡死 | 技出·承与善谋双翻面 | 额外触发不翻面 | 事实20 |
| 反间不终止 | 终止判定写在入队事件同一步 | 判定挪到下一步 | 事实19 |
| 情势无限连锁 | 嵌套 useCard 未入 getHistory | 选项执行计数 | 事实19 |
| 爵制①②失效 | addSkill 指向已删 id | storage 标记门控 | 合并收尾 |
| 三将显示英文 id | translate 漏武将名条目 | 补条目 | translate 三件套 |

另：session 侧两事故（删日志破坏 seq 连续性 / 助手载体缺 turn/step 冻结 meter）
见 `docs/开发心得.md` 的扩展外围篇与 dsh-session-neutralize-orphans.mjs 工具头注。

---

## 5. 神威技接入清单（新武将含神威技时逐项打勾）

1. 技能 `init` 写 `player.storage.tdgx_sw[技能名] = 1`；
2. `filter` 查 `tdgx_sw[技能名] > 0`，`content` 开头自减；
3. `tdgx_shenwei_kill` 的 **filter 与 content 两处 list** 都要加技能名；
4. 该隐藏技 `tdgx_shenwei_kill` / `tdgx_turn_reset` **列进武将数组**；
5. 正文自带"每局限一次"字样的，按类别规则（最多2次）实现，正文视为赘述；
6. mod 型配件（如纵横/驰）**不进数组**，动态 addTempSkill/addSkill。

## 6. translate 三件套（每个技能缺一不可）

`技能名` + `技能名_info`（C5 lint 对 sub 技能也要求 _info，缺失判 WARN）；
武将要有 **translate 名字条目**（漏了显示英文 id）+ `characterIntro` 长介绍。

---

## 7. ★★ 步骤号必须连续（兵权卡死事故，2026-09-13 深夜定位）★★

**这是本包迄今为止代价最大、也最容易被忽视的一条引擎约束。**
完整诊断过程见 [`内容步骤机制-踩坑与诊断方法论.md`](内容步骤机制-踩坑与诊断方法论.md)；
此处只记结论。

### 7.1 机制

引擎把 `content` 函数编译成步骤机时（`parsex` / `Legacy`，game.js:12070-12134）：

1. 从 **k = 0 开始逐个**把 `'step k'` 替换成 `break;case k:`；
2. **遇到第一个不存在的编号立即停止**（game.js:12106-12111：
   `var reg = new RegExp("['\"]step "+k+"['\"]"); if(result==null) break;`）；
3. 最后把 `'step 0'` 换成
   `if(event.step==${k}){event.finish();return;}switch(step){case 0:`（game.js:12129、12113）。

⇒ **编号一旦断开，后果是双重的**：

- 断点之后的步骤**根本不会被编成 `case`**，那些代码只会被顺序执行一次；
- 那句"末步守卫"按**错误的 k** 生成，**永远不成立** ⇒ `event.finish()` 永不执行
  ⇒ 主循环把同一段代码**无限重复执行**（`event.step` 无上限增长），整个游戏循环被占死。

官方代码全部遵守这条：`arrangeTrigger`（game.js:15265）用的是 0,1,2,3…。
**连续编号不是风格问题，是能不能运行的问题。**

### 7.2 事故与修复

兵权原写成 **0/3/4/14/16**（当年为了"分支跳转可读"刻意留空号），结果：

| 现象 | 说明 |
|---|---|
| 选中诸葛亮后，轮到他即卡死，什么都没发生 | 游戏主循环被占死 |
| 诊断气泡只有一条，`引擎传step=169407 event.step=169407` 且**持续增长** | step 无限增长 = `event.finish()` 从未被调用 |
| `event.goto(3)` 形同虚设 | switch 都没建起来，goto 改的 step 没有对应 case |

修复：步骤重排为连续 **0,1,2**，三个时机（roundStart / phaseBegin / phaseEnd）
全部内联在 step 0 按 `event.triggername` 分派，**不再使用 `event.goto`**；
送兵循环改为在 step 1 内用 `while` 跳过自己/离场者。

### 7.3 复发防护

`atlas/tools/renumber-steps.mjs`（新增）：扫描全文每个技能 content 的步骤号，
报告"不连续 / 重号"，并可在 `--apply` 时重排为 0,1,2… 且**同步改写 `goto` 目标**。
当前扫描仍报两处待处理：

```
mzy_fanjian   0,1,2,3,4,5,6,8   ← 跳过 7，step 8 在引擎里没有 case
mzy_yingyan   0,1,1,2,3,4,5,6   ← 重号
```

**新建技能时：从 `'step 0'` 起一个一个连排，不要留空号。**
需要"多分支"就在同一步内用 `if (event.triggername …)` / `if (event.step …)` 分派，
不要靠跳号表达结构。


# 新四将开发笔记 · 陆逊 / 名·刘备 / 杜预 / 陆抗（2026-09-12）

> 本文收录 2026-09-12 新增四将的**定稿文本、命名决策、引擎事实核实清单（含 game.js 行号）、
> 卡面歧义的实现取舍、神威技类别机制与实现进度**。
> 引擎行为结论全部对着本机琉璃版 5.5 `resources/app/game/game.js`（65803 行）当次核实，
> 行号随版本会漂移，机制契约以结论为准。

---

## 1. 四将定稿文本（手写卡校准稿）

### 陆逊　四血　吴

- **连营**：锁定技，每当你因非使用和打出而失去牌时，你获得一个"谦"标记。当你没有牌时，你将手牌数摸至体力上限。出牌阶段开始时，你摸等同于"谦"数量的牌，并弃置所有的"谦"。结束阶段，若你使用或打出的牌不大于你的体力值，你摸等同于你弃牌阶段弃置牌的数量。
- **炽炎**：出牌阶段限X次（X为游戏轮次），你可以弃置等同于你当前体力值的牌，并对一名角色造成一点火焰伤害，然后你可以弃置一张牌，视为使用【铁索连环】。结束阶段，若你以此法造成的伤害不小于你的体力值，你可以对所有处于连环状态的角色造成一点火焰伤害，并弃置其装备区内的所有牌。
- **谦逊**：锁定技，当你受到伤害时，若你的"谦"小于体力上限，你进行一次判定，若结果为红色，你摸两张牌；若你的"谦"大于体力上限，你可以弃置等同于你体力值的"谦"，你令此伤害-1；若你的"谦"等于体力上限，你免疫此伤害。
- **彰才**（神威技）：出牌阶段，你使用牌无次数和距离限制。

### 名·刘备　四血　蜀

- **仁德**：游戏开始时，你获得3个"仁"标记。回合开始时，你收回所有的"仁"。出牌阶段，你摸等同于你身上"仁"的数量的牌。当一名拥有"仁"的角色被牌指定时，你可以失去一点体力或两张牌，令此牌无效（每回合限一次）。结束阶段，你可以将你身上的"仁"分配给任何不同的角色。
- **章武**（神威技）：每局游戏限一次，回合开始时你多执行一个出牌阶段，且本回合使用牌无次数限制。
- **兴汉**（主公技）：游戏开始时，你获得一个"仁"。当蜀势力角色对你造成伤害时，你免疫。（每角色每回合限1次）

### 杜预　四血　群

- **武库**（锁定技）：当场上有人上装备时，你获得一个"备"标记，并摸一张牌。每回合限一次，你可以消耗一个"备"标记，将一张区域内的牌当非装备牌使用或打出。（"备"标记数量上限为5）
- **破竹**：每回合限一次，出牌阶段，你可以选择一种牌名，你本回合可以无次数与距离限制地使用此牌。若你本回合使用此牌造成过伤害，本游戏你使用此牌名无次数限制并无距离限制。
- **振鞘**（锁定技）：当你装备武器牌时，你使用牌无法被响应。当你造成伤害时，你可以令其免疫此伤害，并获得其装备区内的所有牌。当你使用【杀】时，此【杀】的伤害+X（X为你装备牌的攻击范围-你的体力值，且X最小为0）。
- **灭吴**（神威技）：每局游戏限一次，出牌阶段，你可以摸X张牌（X为标记数量+你的体力上限）。

### 陆抗　四血　吴

- **毁堰**：出牌阶段，你可以选择一个区域令其失效并获得对应效果：①武器区：使其造成的伤害+1；②防具区：每回合第一次受到伤害时，你免疫此伤害；③进攻马：使用牌无法被响应；④防御马：摸两张牌；⑤判定区：无视距离并使用牌无次数限制；⑥手牌区：分配X点伤害给任意角色（X为你的体力值-1）。
- **抗晋**（锁定技）：当你受到体力值不小于你的角色造成的伤害时，你可以弃一张牌，并进行一次判定，如果颜色为红，你免除此伤害。当你造成伤害后，你可以令一个区域的状态本轮与你相同，然后你选择装备区内的一项恢复。
- **背水**（神威技）：每局游戏限一次，出牌阶段，你可以恢复你的所有区域。

---

## 2. 神威技类别（用户定义，已录入 README）

> 锁定技，当你击杀一名角色时，此技能使用次数+1，每局游戏限一次。

落地口径（经用户澄清）：

- 神威技**初始使用次数为 1**（开局即可用一次）；
- 你**首次击杀**角色时使用次数 +1（该加成每局游戏只触发一次，之后再杀不叠加）；
- 一局内至多 **2 次**。首杀加次数为锁定效果，自动生效。

⚠ 正文矛盾处理：章武/灭吴/背水的正文自带"每局游戏限一次"，与类别规则（最多 2 次）冲突，
**按类别规则实现**（正文里的那句视为赘述）。

### 实现模型（extension.js）

- 记账位：`player.storage.tdgx_sw[技能名]` = 剩余次数（各技能 `init` 置 1）；
  `player.storage.tdgx_sw_bonus[技能名]` = 是否已领首杀加成。
- `tdgx_shenwei_kill`（隐藏技，`trigger:{source:'dieAfter'}`，forced）：击杀者为技能主人时，
  给每个未领加成的神威技 +1 次。
- 各神威技 `filter` 检查剩余次数、`content` 开头扣减。
- 为什么不用 `limited:true`：引擎的限定技是"一次性"模型（awakenSkill 退役），
  撑不起"1 + 首杀1"的两次模型，故手工记账。

---

## 3. 命名与注册决策

| 项 | 取值 | 理由 |
|---|---|---|
| 武将 id | `tdgx_luxun` / `tdgx_liubei` / `tdgx_duyu` / `tdgx_lukang` | 官方包已有 `luxun`/`duyu`/`lukang`，直接用会撞 `lib.character` 全局表 |
| 技能前缀 | `lx_` / `mlb_` / `dy_` / `lkang_` | 对照 `atlas/index/packs.json`（34 包 / 5227 技能）全库查重，四个前缀均无冲突 |
| 共用隐藏技 | `tdgx_shenwei_kill` / `tdgx_turn_reset` | 类别机制载体；**列进每个武将数组**（心得 §4.3：不列进数组 filterTrigger 直接跳过） |
| 显示名 | 陆逊 / 名·刘备 / 杜预 / 陆抗 | "名·刘备"称号沿用校准稿原文（首字曾存疑，用户校准为"名"） |

`tdgx_turn_reset`（每回合开始幂等复位）：清 `mlb_rd_used`（仁德每回合限一次）、
`mlb_xh_log`（兴汉每角色每回合限一次）、回合拥有者的 `lx_cy_used`（炽炎限轮次次计数）。

---

## 4. 引擎事实核实清单（当次逐条对着 game.js / character 包核实）

| # | 结论 | 证据位置 | 用在 |
|---|---|---|---|
| 1 | `disableEquip(n)` / `enableEquip(n)` 接受数字/字符串/数组，**只支持 equip1–5**；废除时引擎自动把栏内牌置入弃牌堆 | game.js:21916/21948（创建）、13309/13356（content） | 毁堰、抗晋、背水 |
| 2 | `equipAfter` / `equipEnd` 时机存在，`global:'equipAfter'` 有先例 | shiji.js:4957、refresh.js:12566 | 武库① |
| 3 | 击杀时机用 `trigger:{source:'dieAfter'}`，`event.source` 即击杀者 | refresh.js:14226、ow.js:2331、sp.js:12904 | 神威充能 |
| 4 | 主公技字段 `zhuSkill:true`，非主公时引擎在触发链直接跳过（`isZhu2()`） | game.js:22930/23091 | 兴汉 |
| 5 | 攻击范围：`player.getAttackRange()`（含武器 attackFrom/attackRange 计算） | game.js:28980 | 振鞘③ |
| 6 | **`usable` 只认静态数字**（`typeof info.usable=='number'`），动态"X次"必须手工记账 | game.js:15528、33066 | 炽炎 |
| 7 | **使用与打出的 lose 事件 `type` 都是 `'use'`**（useCard 与 respond 两条路径同值） | game.js:19152、20063 | 连营①："非使用和打出" = `type!='use'` |
| 8 | `addTempSkill(skill)` 缺省过期时机 `['phaseAfter','phaseBefore']` = 本回合结束 | game.js:28663 | 章武、破竹·turn |
| 9 | "弃牌阶段弃置的牌"判据：lose 事件 `type=='discard'` 且父链 `getParent('phaseDiscard')` | diy.js:2445（逐字先例） | 连营④ |
| 10 | `mod.cardEnabled` 存在（查询期拦截"能否使用"） | game.js:33158 + gujian.js:1795 等 | 毁堰⑥手牌区 |
| 11 | 连环状态：`player.isLinked()` | game.js:29317 | 炽炎·燎原 |
| 12 | `useCardToPlayered` / `useCardToTargeted` 是**逐目标子事件**（`emptyEvent`），其 `excluded` / `directHit` 数组与父 useCard 事件**共享**（`next.excluded=event.excluded`） | game.js:19451-19489 | 仁德·庇护（整牌无效）、振鞘①/毁堰③（无法被响应） |
| 13 | 整牌无效的惯用法：往父事件 `excluded` 里塞目标（引擎 `all_excluded` 路径中止结算） | sb.js:2463、collab.js:932 | 仁德·庇护 |
| 14 | "无法被响应"惯用法：`trigger.directHit.add(...)` | 范式库 A3 `yjzhenlve`（`directHit.addArray`） | 振鞘①、毁堰③ |
| 15 | `getHistory('useCard')` 与 `getHistory('respond')` 均合法（respond 在 game.js:20060 入史） | collab.js:723 | 连营④、破竹·定势 |
| 16 | 虚拟牌使用：`player.chooseUseTarget({name:'x',isCard:true},...)`；转化视图 `get.autoViewAs({name:'tiesuo'},cards)` | diy.js:4287、ddd.js:103 | 炽炎①、武库·启备 |
| 17 | 火焰伤害调用 `target.damage(1,'fire')` | sb.js:4816、tw.js:7250 | 炽炎 |
| 18 | `chooseCard` 不支持判定区 `'j'`（提示词与建牌只认 h/e；判定区只能 `choosePlayerCard`） | atlas C11（本仓实测） | 武库·启备限定 'he' |
| 19 | `player.countEnabledSlot('equipN')` 可查栏位可用数 | game.js:13317（disableEquip content 内） | 抗晋·同轨防误恢复 |
| 20 | `player.getEquip(1)` 判"装备着武器" | 全库通行写法 | 振鞘 |

---

## 5. 卡面歧义 → 实现取舍（待用户过目确认）

### 陆逊
| 歧义点 | 取的口径 |
|---|---|
| "失去牌时获得一个谦"按次还是按张 | **按次**：一次失去任意张 = 1 个谦 |
| "当你没有牌时" | 手牌+装备+判定**全部为空**时触发（挂 loseAfter，含使用打出的路径） |
| 伤害-1 减到 0 以下 | `Math.max(0, num-1)`（减为 0 即无伤害，不出现负数） |
| 炽炎②"以此法造成的伤害" | 只统计炽炎①主动技造成的火伤（伤害事件打 `lx_cy` 标签） |

### 名·刘备
| 歧义点 | 取的口径 |
|---|---|
| "令此牌无效" | **整张牌无效**（父 useCard 事件全部目标进 excluded），非仅对该角色无效 |
| "每回合限一次" | 按全局回合计（任何人的回合开始时复位） |
| 仁德保护范围 | 任何**非刘备本人**使用的牌指定"仁"持有者时都可触发 |
| 兴汉开局+1仁 | 并入仁德开局结算时判 `isZhu2()` 发放（非主公不给） |

### 杜预
| 歧义点 | 取的口径 |
|---|---|
| 武库"一张区域内的牌" | **自己的手牌+装备区**（判定区因 chooseCard 不支持而排除） |
| 武库"使用或打出" | 出牌阶段**使用**路径已实现；"打出"（响应路径）**未实现** |
| 破竹"选择一种牌名" | 从**当前手牌的牌名**里选（否则候选集无限大） |
| 破竹永久解锁的判定 | 本回合内以该牌名**造成过伤害**（回合结束阶段检查） |
| 振鞘②"令其免疫" | 免疫整次伤害并夺其装备区所有牌，时机 damageBegin（`trigger.cancel()`） |
| 振鞘③"当你使用【杀】时" | 落在**杀造成伤害时**加值（含转化杀，`viewAs||name` 判名） |
| 灭吴 X | 「备」标记数 + 体力上限（标记不消耗） |

### 陆抗
| 歧义点 | 取的口径 |
|---|---|
| 毁堰次数 | 无出牌阶段限次；但**每个区域整局只能选一次**（六选一自然封顶 6 次）；被背水/抗晋恢复的栏位可再选 |
| 判定区"失效" | 弃置判定区内现有的牌 + **置 `storage._disableJudge` 真正拦住后续放置** + 获得⑤效果。引擎原生支持：`canAddJudge` 首行即 `if(this.isDisabledJudge()) return false;`（`game.js:26826`），故无需自造 mod。恢复侧（抗晋单栏/轮清理/背水）需一并清零，并用 `lkang_judge_own` 标记归属以免误恢复他人废除的判定区 |
| 手牌区"失效" | 手牌**不可使用**（mod cardEnabled 拦 h 区）+ 立即⑥分配伤害；手牌仍可被摸到/弃置 |
| 防具区"每回合第一次受伤" | 以本回合已完成的 damage 历史数 = 0 判定 |
| 抗晋"令一个区域的状态本轮与你相同" | 复制的是你**已失效**的区域（复制未失效区域无意义）；装备栏复制=目标该栏本轮废除（轮开始自动恢复，且只在目标该栏原本可用时才废除，避免误恢复对方自废的栏）；判定/手牌复制=目标获得对应失效状态 |
| 抗晋"选择装备区内的一项恢复" | 恢复**你自己**的武器/防具/进攻马/防御马四栏之一 |
| 背水"恢复所有区域" | 恢复四个装备栏 + 清空判定/手牌区失效状态 |

---

## 6. 实现进度

### 6.1 本次补完（2026-09-12 第二轮）

| 项 | 改动 | 证据 / 说明 |
|---|---|---|
| C5 WARN 清零 | 补 `translate['dy_wuku_use_info']` | lint C5 判据是 `lib.translate[skill+'_info']` 存在；`sub` 技能缺它会被判 **WARN**（不是 INFO）。基线由 `0 ERROR / 1 WARN` → `0 ERROR / 0 WARN` |
| **§6-2 毁堰判定区真失效** | 废判定区时置 `player.storage._disableJudge = true` + `player.update()` | `game.js:22013-22015` `isDisabledJudge(){ return Boolean(this.storage._disableJudge); }`；`canAddJudge` **第一行**就是 `if(this.isDisabledJudge()) return false;`（`game.js:26826`）⇒ 只弃置现有判定牌**不够**，置位后引擎在**所有**放置路径直接拒绝。与官方 `_disableJudge` 技能同机制（`game.js:14732`） |
| 同上·恢复路径补齐 | 抗晋单栏/轮清理/背水三处恢复判定区 | 缺任一处，背水或抗晋就"救不回"判定区 |
| **归属保护** | 新增 `storage.lkang_judge_own` | `_disableJudge` 是**引擎全局开关**，别的技能也可能合法废除判定区。无条件清零会误恢复他人的废除 ⇒ 置位时标记归属，恢复时仅在"是我设的"才清 |
| 抗晋·复制判定区 | 复制时同样置 `_disableJudge` + 归属标记 | 原先只置 `lkang_copy.judge` 并弃牌，目标判定区仍可被继续放延时锦囊 |

**验证**：`check-encoding.mjs`（无 BOM / 统一 LF / 语法通过）、`lint-extension.mjs` **13 项全过 0 ERROR 0 WARN**、
`mock-load.mjs` 断言全过、`verify-extension.mjs` 双分支 0 失败，另加一次**结构性核查**（9 项）：
归属标记置/清成对、每处 `_disableJudge` 清除都在守卫内、三条恢复路径都处理判定区、两条废除路径都置开关。

### 6.2 既有进度（第一轮）

- ✅ README「技能类别：神威技」
- ✅ 武将条目 / characterIntro / translate（技能名+_info+标记文案）全部入 extension.js
- ✅ 神威机制：tdgx_shenwei_kill / tdgx_turn_reset
- ✅ 陆逊全套：lx_lianying（+draw/end 子技）、lx_chiyang（+end）、lx_qianxun、lx_zhangcai（+mod）、lx_qian 壳
  （过程中修掉一处铁律二违规：lx_lianying_end 曾引用未定义闭包函数 countDiscard，已内联）
- ✅ 刘备 / 杜预 / 陆抗技能本体（mlb_* / dy_* / lkang_*，含 mlb_ren / dy_bei 标记壳）

### 6.3 仍待办

1. **武库的"或打出"（响应期视为打出）路径 —— 未实现**。
   核实结论：需要**另建一个 viewAs 形态技能**，不能改现有的 `dy_wuku_use`。
   引擎的范例是它自己的 `aozhan_sha`（`game.js:34153-34179`）：
   ```
   enable:['chooseToUse','chooseToRespond'],   // 两会话期都可用
   filterCard / viewAs / viewAsFilter,          // 转化定义
   position:'hs', prompt:'…使用或打出',
   onrespond:function(event,player){ … }        // 打出时结算「备」消耗
   ```
   要点：`onrespond` 在 respond 事件里触发（`game.js:20010-20012`），
   **消耗「备」应放这里**，而不是 content（viewAs 技能没有独立 content 执行路径）。
   须先定：打出时长用什么牌名（若给"任何非装备牌"则等于万能响应，明显过强）；
   以及"使用/打出共用一次限次"如何记账。
2. 四将暂无立绘（[4] 缺省，引擎补空数组）。

---

## 7. 复验命令（改完 extension.js 后照跑）

```bash
GAME="/c/Users/luoti/Desktop/三国杀·琉璃版5.5【电脑版】（修复清正）/resources/app"
node --check extension/extension.js
node atlas/tools/lint-extension.mjs extension/extension.js --app "$GAME" --index atlas/index
node ../check-encoding.mjs extension/extension.js
```

进游戏验收：重启 → 启用「天地归一」→ **开新局** → 选将界面选四将之一，
重点核对：神威技首次击杀后提示"使用次数+1"；毁堰六个区域的即时效果；仁德的收发循环。

feat: 范式抽取器 idiom.mjs + 撤回「17 处一致」的错误断言

新增 atlas/tools/idiom.mjs —— 从全库 5232 个技能里挖「做 X 的标准写法」
  --idiom <名称>   查内置惯用法（额外出牌阶段/跳过弃牌阶段/伤害+1/防止伤害/
                   加标记/限定技/觉醒技/锁定技但可选发动/拼点/额外回合/转换技/…）
  --find <正则>    按正则找技能，再按实现签名聚类
  --mine           全库高频调用序列 n-gram 挖矿
  语料来源：packs.json 的 line/endLine 按行区间切片，无需重新解析武将包。

═══ 本工具第一次运行就推翻了我自己的断言 ═══

我在 B14b 里写过「本包 17 处额外出牌阶段全部是 trigger.next.push，
没有一处用 getParent()」——**这是错的**。当时我只读了 sb.js / jsrg.js 两处，
就推广到全部 17 处：**抽样代替普查**。

全库聚类后的真实分布（17 个技能）：
  event.next.remove → trigger.next.push              8 处
  event.next.remove → trigger.getParent() → next…    7 处
  其他机制（insertPhase / 直接 phaseUse）             2 处
即 8 : 7，**不存在压倒性写法**。

代码选择本身不变，但依据换了：不是「17 处先例」，而是引擎主循环语义 ——
event.next 队列在「当前事件每一步之间」被消费（game.js 41700 附近），
trigger 是触发事件自身（game.js:41675 trigger=event._trigger，
{player:'phaseBegin'} 的触发事件即 phaseLoop），挂 trigger.next 落在
phaseLoop 的 step 7 与 step 8（建立 phaseList，game.js:15965-15968）之间，
正是「紧接着回合开始」；而 trigger.getParent().next 是父事件队列，
要等 phaseLoop 整体跑完，即排到整个回合之后。

已撤回该断言的 5 处落点：
  extension/extension.js  文件头 B14 条目
  extension/extension.js  mgj_extra_phase 的 content 注释
  docs/开发心得.md        §4.6 勘误（重写为两派分布 + 引擎判据）
  docs/开发心得.md        §六 速查表该行
  atlas/tools/idiom.mjs   工具自述

═══ 聚类算法的两次返工（都是实测暴露的）═══

1. 按「整个技能」算签名 → 大技能各自唯一，17 个技能聚成 17 类，等于没聚。
2. 改按「匹配行 ±5/±7 行的行窗口」→ 前置的无关调用（player.phaseDraw 等）
   混进签名，还是碎成 14 类。
3. 最终改为**锚定**：先定位匹配的那个调用链，再取 chains[anchor .. anchor+2]。
   且锚定窗口必须用**不去重**的调用序列 —— 去重版会让技能前面已出现过的
   event.next.remove 从窗口里消失，签名被截断成 player.phaseUse，
   把 3 个 getParent 派错误并箱（实测踩到）。

另：工具在最大派不过半时明确输出「⚠ 无压倒性写法，不要盲抄多数派」，
不做「有主导写法」的假象。

═══ 顺带扩写 ═══
atlas/tools/README.md 增补 idiom.mjs 章节（三条设计要点）与 see.ps1 兜底说明。
